sudo: required

services:
  - docker

before_install:
  - docker build -t pupperemeritus/docker-react-example -f Dockerfile.dev .

script:
  - docker run  -e CI=true pupperemeritus/docker-react-example npm run test

